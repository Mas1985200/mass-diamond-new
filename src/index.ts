export { default as App } from "./App";

export {
  APP_METADATA,
  DEFAULT_LOCALE,
  LOCALES,
  LOCALE_BY_CODE,
  CAPABILITIES,
  CAPABILITY_BY_ID,
} from "./config";

export type {
  CapabilityDefinition,
  CapabilityId,
  AppDirection,
  AppMetadata,
  LocaleConfig,
  SupportedLocale,
} from "./config";
